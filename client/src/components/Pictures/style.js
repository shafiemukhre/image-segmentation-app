import { makeStyles } from "@material-ui/core/styles";


const useStyles = makeStyles(() => ({
    demo: {
        width: '50px',
        height: '50px',
        backgroundColor: 'red',
        textAlign: 'center',
    },
}));

export default useStyles;